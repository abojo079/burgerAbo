

<!--ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
<ul>



<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul-->

<body>

<form class="create-form">

  <div class="form-group" id="survey">
   
<h3>Eat-Da-Burger!</h3>
    <textarea class="form-control" id="bu" rows="3" columns="1" name="name"></textarea>
  </div>

<br/>
  <button type="submit">Submit</button>
  </br>
  <footer>Enter a burger you want to eat and click the "Submit" button</footer>
</form>
</body>